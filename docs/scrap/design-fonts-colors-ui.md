# デザイン調整：フォント・カラー・UI（issue #24）

## フォント整理

- ベースフォントを `Klee One` に変更（会話パート・ラベル・UIテキスト全般）
- タイトル・お品書きは引き続き `New Tegomin`
- サブタイトルの `Zen Kurenaido` を廃止し `Klee One` に統一（フォント数を4→3に削減）
- 服薬指導箋・副作用レポートの本文は `Shippori Mincho`（明朝体で書類らしさを演出）

## カラーパレット

- 背景色を `#040810`（青黒）→ `#2a2d2b`（ダークグレー／夜の路地感）に変更
- 左右の暗いグラデーションも青系から `rgba(10,10,10,0.8)` に統一
- 柴犬茶 `--shiba-brown: #b87040` をトークンに追加
- 進む系ボタン → セージ枠・ホバーでセージ背景
- 戻る系ボタン → 茶色枠・ホバーで茶色背景

## チャット形式の入力UI

- 薬剤師アイコン（左）＋ グリーティングテキスト のチャット形式を入力ページに適用
- ユーザーアイコン（丸型、右）＋ テキストエリア（左）＋ 送信ボタン（テキストエリア右下）
- テキストエリア右上に × アイコンで入力クリア（`clear_controller.js` 追加）
- 副作用ページの「クリア」ボタンも × アイコン化

## アイコン

- 薬剤師アイコン（`pharmacist.png`）を高画質版に差し替え
- ユーザーアイコン（`user-icon.png`）にネオンカクテルグラス画像を使用
- 両アイコンに白いリング＋ぼかしグロー（`box-shadow`）を追加
- `mask-image` → `border-radius: 50%` に変更（box-shadow が正しく描画されるように）

## 処方箋・副作用レポートページ

- AIレスポンスを「薬剤師より：」で分割し、会話部分はチャット形式で表示
- 「【服薬指導箋】」「【今日の副作用レポート】」をカード外にタイトルとして分離
- 本文（薬剤名・分類など）はカード内に `Shippori Mincho` で表示
- original-topic をフラット表示に変更（`.original-topic-flat`）
- シェアボタンをカード右上に移動・小型化（`.btn-share-sm`）

## ヘッダー・フッター

- ヘッダー右端に問い合わせ・X・GitHub のアイコンリンクを追加
- フッターにサイト名＋コピーライトを追加
